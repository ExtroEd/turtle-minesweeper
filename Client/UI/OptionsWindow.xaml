<local:BaseWindow x:Class="Client.UI.OptionsWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:local="clr-namespace:Client.UI"
                  Title="Turtle Minesweeper">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/UI/ButtonStyles.xaml"/>
                <ResourceDictionary Source="/UI/MainStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <TextBlock Style="{StaticResource TitleTextStyle}" x:Name="TitleText"/>
        <TextBlock Style="{StaticResource SplashTextStyle}" x:Name="SplashText"/>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="Window Mode" FontWeight="Bold" Margin="0,10,0,5"/>
            <ComboBox x:Name="WindowModeComboBox" SelectedIndex="0" Margin="0,0,0,20">
                <ComboBoxItem Content="Fullscreen" />
                <ComboBoxItem Content="Fullscreen Windowed" />
                <ComboBoxItem Content="Windowed" />
            </ComboBox>

            <Button Content="Apply Window Mode" Style="{StaticResource MainButtonStyle}" Click="ApplyWindowMode_Click"/>
            
            <Button Content="Back" Style="{StaticResource RedButtonStyle}" Click="Back_Click"/>
        </StackPanel>

        <!-- Version text -->
        <TextBlock Style="{StaticResource VersionTextStyle}"/>
    </Grid>
</local:BaseWindow>
